<%- partial('_partial/disqus', { index: true }) %>
<%- partial('_partial/archive', {page: page}) %>
<% if (page.total > 1) { %>
    <nav id="pagenavi">
        <% if (page.prev) { %>
            <a href="/<%- page.prev_link %>" class="prev">Prev</a>
        <% } %>
        <% if ( page.next ) { %>
            <a href="/<%- page.next_link %>" class="next">Next</a>
        <% } %>
    </nav>
<% } %>
